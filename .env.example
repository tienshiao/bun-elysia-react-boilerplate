# Database connection string
DATABASE_URL=postgres://user:password@localhost:5432/dbname

# JWT keys (PEM-encoded). If not set, falls back to keys/*.pem files.
# JWT_PRIVATE_KEY=<PKCS8 private key PEM>
# JWT_PUBLIC_KEY=<SPKI public key PEM>

# Server port (default: 4000)
# PORT=4000
